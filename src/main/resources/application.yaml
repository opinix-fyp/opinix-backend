spring:
  application:
    name: backend

  profiles:
    active: mock

  datasource:
    url: jdbc:mysql://localhost:3306/opinix_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kuala_Lumpur
    username: opinix_user
    password: "MyStrongPass123!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  ml:
    sentiment:
      url: http://localhost:8000/sentiment
